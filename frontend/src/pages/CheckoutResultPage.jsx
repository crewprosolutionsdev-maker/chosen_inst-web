import { Link, useSearchParams } from 'react-router-dom';
export function CheckoutResultPage(){const[params]=useSearchParams();const status=params.get('status');const copy=status==='success'?['¡Pago aprobado!','Gracias por tu compra. Te contactaremos con los detalles del envío.']:status==='pending'?['Pago pendiente','Te avisaremos apenas Mercado Pago confirme la operación.']:['No pudimos completar el pago','Podés volver al carrito e intentarlo nuevamente.'];return <main className="page container checkout-success"><span className="eyebrow">ESTADO DEL PEDIDO</span><h1>{copy[0]}</h1><p>{copy[1]}</p><Link className="btn btn-dark" to="/productos">VOLVER A LA TIENDA</Link></main>}
