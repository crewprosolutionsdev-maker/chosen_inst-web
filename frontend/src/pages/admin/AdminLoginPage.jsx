import { useState } from 'react';
import { Navigate, useNavigate } from 'react-router-dom';
import { adminAuth, adminService } from '../../services/adminService';
export function AdminLoginPage() { const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); const navigate=useNavigate(); if(adminAuth.token()) return <Navigate to="/admin/productos" replace/>; const submit=async event=>{event.preventDefault();try{setLoading(true);setError('');const {token}=await adminService.login(password);adminAuth.save(token);navigate('/admin/productos');}catch(error){setError(error.message);}finally{setLoading(false);}}; return <main className="admin-login"><form onSubmit={submit}><img src="/assets/chosen-logo.webp" alt="Chosen"/><span>PANEL DE ADMINISTRACIÓN</span><h1>Ingresar</h1>{error&&<p className="admin-error">{error}</p>}<label>Contraseña<input type="password" value={password} onChange={event=>setPassword(event.target.value)} autoFocus required/></label><button className="admin-primary" disabled={loading}>{loading?'Ingresando…':'INGRESAR'}</button></form></main>; }
