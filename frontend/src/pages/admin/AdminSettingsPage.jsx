import { useEffect, useState } from 'react';
import { adminService } from '../../services/adminService';
const initial={shipping:{flatRate:0,freeThreshold:0,pickupEnabled:true,pickupLabel:'Retiro a coordinar'},transfer:{enabled:true,alias:'',cbu:'',holder:''},mercadoPago:{enabled:true}};
export function AdminSettingsPage(){const[form,setForm]=useState(initial);const[message,setMessage]=useState('');const[error,setError]=useState('');const[saving,setSaving]=useState(false);useEffect(()=>{adminService.settings().then(data=>setForm({shipping:{...initial.shipping,...data.shipping},transfer:{...initial.transfer,...data.transfer},mercadoPago:{...initial.mercadoPago,...data.mercadoPago}})).catch(e=>setError(e.message));},[]);const section=(name,key,value)=>setForm(current=>({...current,[name]:{...current[name],[key]:value}}));const submit=async event=>{event.preventDefault();try{setSaving(true);setError('');setMessage('');const data=await adminService.saveSettings(form);setForm({shipping:data.shipping,transfer:data.transfer,mercadoPago:data.mercadoPago});setMessage('Configuración guardada.');}catch(e){setError(e.message);}finally{setSaving(false);}};return <section className="admin-page"><header><div><span>CHECKOUT</span><h1>Envíos y pagos</h1><p>Configurá las opciones que verá el cliente al finalizar su compra.</p></div></header><form className="admin-form admin-settings" onSubmit={submit}>{error&&<p className="admin-error">{error}</p>}{message&&<p className="admin-success">{message}</p>}<h2>Envíos</h2><div className="admin-form-row"><label>Tarifa nacional<input type="number" min="0" value={form.shipping.flatRate} onChange={e=>section('shipping','flatRate',e.target.value)}/></label><label>Envío gratis desde<input type="number" min="0" value={form.shipping.freeThreshold} onChange={e=>section('shipping','freeThreshold',e.target.value)}/></label></div><label><input type="checkbox" checked={form.shipping.pickupEnabled} onChange={e=>section('shipping','pickupEnabled',e.target.checked)}/> Permitir retiro</label><label>Nombre del retiro<input value={form.shipping.pickupLabel} onChange={e=>section('shipping','pickupLabel',e.target.value)}/></label><h2>Transferencia</h2><label><input type="checkbox" checked={form.transfer.enabled} onChange={e=>section('transfer','enabled',e.target.checked)}/> Habilitar transferencia</label><label>Titular<input value={form.transfer.holder} onChange={e=>section('transfer','holder',e.target.value)}/></label><div className="admin-form-row"><label>Alias<input value={form.transfer.alias} onChange={e=>section('transfer','alias',e.target.value)}/></label><label>CBU/CVU<input value={form.transfer.cbu} onChange={e=>section('transfer','cbu',e.target.value)}/></label></div><h2>Mercado Pago</h2><label><input type="checkbox" checked={form.mercadoPago.enabled} onChange={e=>section('mercadoPago','enabled',e.target.checked)}/> Habilitar Mercado Pago</label><small>El Access Token se guarda de forma segura en Render, nunca en esta página.</small><button className="admin-primary" disabled={saving}>{saving?'GUARDANDO…':'GUARDAR CONFIGURACIÓN'}</button></form></section>}
