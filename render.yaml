services:
  - type: web
    name: chosen-inst-web
    runtime: node
    plan: free
    region: ohio
    branch: main
    buildCommand: npm run render-build
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 22.22.0
